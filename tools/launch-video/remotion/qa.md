# Render evidence — 2026-09-17, interaction revision

[Review preview](https://2280d57f.komo-wb5.pages.dev/promo/) · [PR #10](https://github.com/tjcages/komo/pull/10) · [OFF-672](https://linear.app/off-brand-studio/issue/OFF-672)

- H.264 MP4: 1920×1080, 30 fps, 420 frames, exactly 14 seconds, 2,284,173 bytes. Silent.
- SHA-256: `bf856007b70062a47c4497b051422d57a06e64bfde3c24249b1001db951d792c`.
- Eight deterministic scene clips, concatenated without re-encoding. Sidebar and feed share one continuous camera timeline; drawer and copy meet at the same scale and state.
- Inspected native reactions at frame 48, wide sidebar at 12, incoming comments and search, vertical drawer, prompt paste and sent-message states, and the symbol/wordmark outro. Inspected a 28-frame contact sheet extracted from the final MP4.
- Reaction buttons no longer clip. Reactions replace the native smiley inline. Native thread markup and search controls retained. No sidebar reaction overlays.
- Prompt is generated by the production `agentPrompt` function with the fixture thread text, authors, and replies. The agent UI is a generic light composer; send creates a fixture user message only.
- Loop uses the same exterior background throughout. Source first/last frames are blank; H.264 decoded boundary frames differ only by negligible compression noise (89.64 dB PSNR).
- QC boundary warnings are reviewed: blank-to-blank fades are intentional between conversation/sidebar and agent/logo; drawer/copy intentionally share the same frame state. Wide exemptions cover the zoomed review workspace and expanded drawer, while the text and controls remain legible.
- Remotion TypeScript, edit validation, coverage and opening-title reading hold pass. Repository build, typecheck, all 111 tests in 18 files, and package size gates pass. Rendering dependencies remain outside the workspace.

Render source and commands: README.md. Ignored outputs: `tools/launch-video/output/remotion/komo-promo-14s.mp4`, `thumbnail.png`, `contact-sheet.png`. Creative acceptance remains In Review; no merge or social publication.
